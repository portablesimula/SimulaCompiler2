
begin
	class brecord; begin
		ref(quantity) favirt;
		ref(quantity) fpar;
	end;
	
	class quantity; begin
		ref(quantity) next;
	end;

      class L2Coder;
      begin
            ref(quantity) pq;
            ref(brecord) cbrec;

              inspect cbrec do begin
                  inspect favirt do begin

%                     pq:-fpar.next;
                     pq:-fpar;
                     
                  end;
              end inspect cbrec;

      end L2Coder;


end;
