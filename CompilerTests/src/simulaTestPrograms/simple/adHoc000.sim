
begin

	
	class A(i,r,lr,c); integer i; real r; long real lr; character c; begin
	end;   

	A class B(x,y); long real x,y; begin
		outfix(lr,4,14);
		outchar(c);
	end;
	
	procedure P(lab); name lab; label lab; begin 
		goto lab;
	end;
 
 	new B(11,22.3,44.78,'C',45.21,99.45);
end;