
begin

	ref(statement) first, last;

	class statement; virtual: procedure recover is boolean procedure recover;;
%	class statement; virtual: procedure recover;
	begin
		ref(statement) next;
	end;
	
	last :- new statement;
	while not last.recover do begin
		last.next:-none;
	end;

end;
